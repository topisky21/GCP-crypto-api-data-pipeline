# GCP---api-data-pipeline
Serverless GCP data pipeline that extracts data from REST APIs, stores raw JSON in Cloud Storage, and loads structured records into BigQuery. Built with Python, Flask, and Cloud Run. Part of a series of API extraction pipelines built on GCP.
